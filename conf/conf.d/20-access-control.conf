server:

    # Bloqueia tudo por padrão
    access-control: 0.0.0.0/0 refuse
    access-control: ::0/0 refuse

    # Localhost
    access-control: 127.0.0.0/8 allow
    access-control: ::1 allow

    # Rede Docker / containers locais
    access-control: 172.16.0.0/12 allow

    # Clientes ISP / PPPoE / CGNAT
    access-control: 100.64.0.0/10 allow

    # Redes internas comuns de provedores e clientes
    access-control: 10.0.0.0/8 allow
    access-control: 192.168.0.0/16 allow
    access-control: 172.0.0.0/8 allow

    # IPv6 clientes (ajuste para o seu prefixo real)
    # access-control: 2804:xxxx::/32 allow

    # Bloqueia redes privadas internas de retorno inesperado
    private-address: 10.0.0.0/8
    private-address: 172.16.0.0/12
    private-address: 192.168.0.0/16
    private-address: 100.64.0.0/10
